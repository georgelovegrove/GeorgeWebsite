const INITIAL_STATE = [];

/*
const initialState = Immutable.fromJS({
  someValue: true,
});
*/

export default function (state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
